# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.claude/
.vscode/
.idea/
*.swp

# Logs / temp
*.log
*.tmp
# image editors leave these beside the file they overwrote
*~

# Recording masters. The site ships the re-encoded pair beside each of these —
# pivot-rotation.{webm,mp4} is 222KB + 327KB against this file's 7.8MB — and a
# repo served by GitHub Pages carries its history forever. Keep the master
# locally, re-encode from it, and let the small files be the committed ones.
assets/img/pivot/pivot-rotation.gif
